---
interface Props {
  title: string;
  subtitle?: string;
  description: string;
  category?: string;
  typeTags?: string[];
  techTags?: string[];
  banner?: string | null;
  bannerAlt?: string;
}

const {
  title,
  subtitle,
  description,
  category = 'Project',
  typeTags = [],
  techTags = [],
  banner,
  bannerAlt = `${title} banner`,
} = Astro.props as Props;

const heroTags = [...typeTags, ...techTags].slice(0, 5);
---

<section class="wix-project-hero" data-parallax-hero data-parallax-travel="56">
  <div class="wix-parallax-frame">
    {banner && (
      <img
        src={banner}
        alt={bannerAlt}
        width="1512"
        height="156"
        loading="eager"
        decoding="async"
        fetchpriority="high"
        class="wix-project-hero-image wix-parallax-image"
        data-parallax-image
      />
    )}
    <div class="wix-project-hero-overlay">
      <div class="wix-project-hero-copy">
        <p class="wix-project-hero-label">{category} case study</p>
        <h1 class="wix-project-title">{title}</h1>
        {subtitle && <p class="wix-project-subtitle">{subtitle}</p>}
        <p class="wix-project-description">{description}</p>
        {heroTags.length > 0 && (
          <ul class="wix-project-hero-tags" aria-label={`${title} tags`}>
            {heroTags.map((tag) => (
              <li>{tag}</li>
            ))}
          </ul>
        )}
      </div>
    </div>
  </div>
</section>
