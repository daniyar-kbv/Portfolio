---
interface Props {
  title: string;
  subtitle: string;
  description: string;
  banner?: string | null;
  bannerAlt?: string;
}

const { title, description, banner, bannerAlt = `${title} banner` } = Astro.props as Props;
---

<section class="wix-project-hero">
  <div class="relative h-full overflow-hidden">
    {banner && (
      <img
        src={banner}
        alt={bannerAlt}
        width="1512"
        height="156"
        loading="eager"
        decoding="async"
        fetchpriority="high"
        class="wix-project-hero-image"
      />
    )}
    <div class="wix-project-hero-overlay">
      <div class="wix-project-hero-copy">
        <h1 class="wix-project-title">{title}</h1>
        <p class="wix-project-description">{description}</p>
      </div>
    </div>
  </div>
</section>
