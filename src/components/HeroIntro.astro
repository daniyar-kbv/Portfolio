---
interface Props {
  name: string;
  role: string;
  label: string;
  bannerImage: string;
}

const { name, role, label, bannerImage } = Astro.props as Props;
---

<section class="wix-home-hero" data-parallax-hero data-parallax-travel="44">
  <div class="wix-parallax-frame">
    <img
      src={bannerImage}
      alt="Portfolio banner"
      width="1512"
      height="222"
      loading="eager"
      decoding="async"
      fetchpriority="high"
      class="wix-home-banner wix-parallax-image"
      data-parallax-image
    />
    <div class="wix-home-banner-overlay">
      <div class="wix-home-banner-copy">
        <p class="wix-home-banner-name">{name}</p>
        <p class="wix-home-banner-role">{role}</p>
        <p class="wix-home-banner-label">{label}</p>
      </div>
    </div>
  </div>
</section>
